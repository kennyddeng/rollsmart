# Firebase
pip install pyrebase

# SQLite 
sudo apt-get install sqlite3
# sudo apt-get install sqlitebrowser # optional, if you want to visualize database

# I2C 
sudo apt-get install i2c-tools # should already be preinstalled on Raspberry Pis
sudo apt-get install python3-smbus # should already be preinstalled on Raspberry Pis